@{
    ViewBag.Title = "Partial Content";
    Layout = "";
    String Valore;
    int i;
    var X = Html.X();
}

@*@(Html.X().Container()
        .Html("I am Page 1")
        .StyleSpec("background-color: green;")
    )*@

@(X.Panel()
        //  .LayoutConfig(new VBoxLayoutConfig() { Align = VBoxAlign.Center, Pack = BoxPack.Center })
         //.Height(400)
        //.Layout(LayoutType.Fit)
        .Layout(Ext.Net.LayoutType.Fit)
        .Region(Region.Center)
        //.Margins("2 2 2 2")
        .Border(false)

        .Loader(
            Html.X().ComponentLoader()
                .AutoLoad(true)
                            .Url(Url.Action("GrillaPacientes"))
                .Mode(LoadMode.Frame)
        )
)




